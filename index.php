<?php
/**
 * @package food
 * @subpackage functions
 */

require_once '../kernel/includes/setup_inc.php';

global $gBitSystem;

$gBitSystem->verifyPackage( 'food' );

// placeholder — no content classes or list pages built yet, see Claude memory
// project_food_package_scoping for the plan. Package exists so it can be installed
// and its schema iterated on.
echo 'Food package installed — no pages built yet.';
